const form = document.querySelector('#searchbarForm');
const searchbar = document.querySelector('#searchbar');
const genreTitle = document.querySelector('.genreTitle');
const section = document.querySelector('.section');
const section2 = document.querySelector('.section2');
const section3 = document.querySelector('.section3');
const container_cards = document.querySelector('.container_cards');
const card = document.querySelectorAll('.card');
const bookTitle = document.querySelector('.bookTitle');
const bookDescription = document.querySelector('.bookDescription');
const container_loader = document.querySelector('.container_loader');

export {form, searchbar, genreTitle, section, section2, section3, container_cards, card, bookTitle, bookDescription, container_loader};